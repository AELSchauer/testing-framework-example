<div class="row">
  <div class="col-md-4 offset-4">
    <%= form_with(url: project_contributions_path, method: :post, local: true) do |f| %>
      <%= render 'shared/error_messages', record: @contribution %>

      <%= f.label :amount %>
      <%= f.number_field :amount, class: 'mb-1 form-control' %>

      <% if @dcpp == "show" %>
        <%= f.check_box :dcpp, class: 'mb-1 form-control' %>
        <%= f.label :dcpp, "Would you like to pay an additional 1% to cover the fees?" %>
      <% end %>

      <% if @nudge == "show" %>
        <%= f.check_box :nudge, class: 'mb-1 form-control' %>
        <%= f.label :nudge, "Would you like to make your gift recurring?" %>
      <% end %>

      <%= f.submit "Go to Checkout", class: "mt-2 btn btn-primary" %>
    <% end %>
  </div>
</div>